import { PageHeader } from "@/components/ui/primitives";

// Wave 2 target — replaced by the dashboard agent.
export function Dashboard() {
  return (
    <div>
      <PageHeader title="Visão Geral" blurb="Panorama do projeto, marcos e modo apresentação." />
      <p className="text-sm text-muted-foreground">Em construção (Wave 2).</p>
    </div>
  );
}
