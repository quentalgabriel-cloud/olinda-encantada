import { PageHeader } from "@/components/ui/primitives";

// Wave 2 target — replaced by the budget agent.
export function Budget() {
  return (
    <div>
      <PageHeader title="Orçamentos" blurb="Custos de ferramentas, equipe e comparativo tradicional × IA." />
      <p className="text-sm text-muted-foreground">Em construção (Wave 2).</p>
    </div>
  );
}
